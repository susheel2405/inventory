server:
  port: 8080

spring:
  application:
    name: avoota_inventory
  datasource:
    url: jdbc:mysql://localhost:3306/avoota
    username: root
    password: legion@2428
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
